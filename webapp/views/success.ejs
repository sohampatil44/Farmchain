<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payment Success</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-emerald-100 min-h-screen flex items-center justify-center">
    <div class="bg-white p-8 rounded-lg shadow-lg max-w-md w-full text-center">
        <h1 class="text-3xl font-bold text-emerald-700 mb-4">Payment Successful!</h1>
        <p class="mb-2">Your booking for <%= listing.name %> is confirmed.</p>
        <p class="mb-2">Days: <%= days %></p>
        <p class="mb-4">Total Paid: ₹<%= amount %></p>
        <p class="mb-4 text-sm">Transaction Hash: <a href="https://sepolia.etherscan.io/tx/<%= txHash %>" target="_blank" class="text-blue-500"><%= txHash %></a></p>
        <a href="/farmer" class="bg-emerald-600 text-white px-4 py-2 rounded">Back to Dashboard</a>
    </div>
</body>
</html>