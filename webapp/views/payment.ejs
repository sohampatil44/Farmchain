<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Payment</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex items-center justify-center">
  <div class="bg-white p-8 rounded-lg shadow-lg max-w-md w-full text-center">
    <h1 class="text-2xl font-bold mb-4">Pay for <%= listing.name %></h1>
    <p class="mb-2">Days: <%= days %></p>
    <p class="mb-4 text-lg font-semibold">Total: ₹<%= amount %></p>

    <button 
      onclick="payWithMethod('debit', '<%= bookingId %>', <%= amount %>, <%= days %>, <%= listing.onChainId %>)" 
      class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded mb-2 w-full">
      Pay with Debit Card
    </button>

    <button 
      onclick="showUpiForm('<%= bookingId %>', <%= amount %>, <%= days %>, <%= listing.onChainId %>)" 
      class="bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded w-full">
      Pay with UPI
      
    </button>

    <!-- 👈 UPI Input Form (hidden initially) -->
    <div id="upi-form" style="display: none; margin-top: 20px;">
      <input type="text" id="upi-id" placeholder="Enter UPI ID (e.g., name@upi)" class="border p-2 rounded w-full mb-2">
      <button id="submit-upi" class="bg-green-500 text-white px-4 py-2 rounded w-full">Submit UPI ID</button>
    </div>

    <p id="status" class="mt-4 text-gray-600"></p>
  </div>

  <!-- Load custom JS after HTML -->
  <script src="https://unpkg.com/ethers@5.7.2/dist/ethers.umd.min.js"></script>
  <script src="/js/payment.js"></script>
</body>
</html>